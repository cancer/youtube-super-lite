Circular channel avatar. `role="channel"` (36px, in cards) or `role="nav"` (24px, sidebar). `dot="live"` shows the red broadcasting dot.

```jsx
<Avatar initial="ぺ" role="nav" dot="live" />
<Avatar src="/ch/x.png" role="channel" />
```
