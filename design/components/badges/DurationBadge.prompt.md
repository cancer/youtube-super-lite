Scrim overlay badge, bottom-right of a thumbnail. Pass a time string, or an `icon` + count for playlist stacks.

```jsx
<DurationBadge>1:11:13</DurationBadge>
<DurationBadge icon={listSvg}>100 本の動画</DurationBadge>
```
