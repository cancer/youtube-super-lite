import * as React from 'react';
/** Red LIVE badge for thumbnails. */
export interface LiveBadgeProps { label?: string; dot?: boolean; style?: React.CSSProperties; }
export function LiveBadge(props: LiveBadgeProps): JSX.Element;
